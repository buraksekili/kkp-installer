# direnv configuration (may contain paths to secrets)
.envrc
.vault-creds.env

.k8c-creds.env
.env

# config.yaml holds host-specific values (domain, email, cluster name, ssh key).
# use config.template.yaml as the example; copy to config.yaml and fill in.
config.yaml
kubeconfig*
preset.yaml
seed-mla.values.yaml
auth
kkp-files/
seeds.yaml

# TLS backup files
tls-backup.yaml
kkp-files/tls-backup.yaml

# KubeOne generated files
kubeone-aws/terraform.tfvars
kubeone-aws/kubeone.yaml
kubeone-aws/tf.json
kubeone-aws/.terraform/
kubeone-aws/terraform.tfstate
kubeone-aws/terraform.tfstate.backup